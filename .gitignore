# Android / Gradle build output
.gradle/
.kotlin/
build/
**/build/
local.properties

# IDE and OS files
.idea/
*.iml
.DS_Store
Thumbs.db

# Local secrets (never commit)
*.jks
*.keystore
*.pem
*.p12
.env
.env.*
!.env.example
# Shared debug key only (same signature on every machine). Not for Play Store.
!keystore/android-debug.keystore

# Generated APKs, reports and local validation artifacts
outputs/
work/
*.apk
*.aab
*.apks
Res/

# Large phrase-pack source downloads (rebuild with tools/build_phrase_pack.py)
phrase-packs/raw/
